# Gold / reference artifacts for cross-language validation.
#
# Add CSVs exported from R weightflow, survey, or published tables.
# Naming: <method>_<case>.csv
# Each recovery/gold test should document source, seed, and tolerances.
#
# Do not mark Recipe.step_* public until the matching gold/recovery test is green.
